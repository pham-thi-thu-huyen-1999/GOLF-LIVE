import tw from 'twin.macro'
import styled from 'styled-components'

const StyleToursDetail = styled.section`
    ${
        tw`
            pt-[80px]
            pb-[80px]
            lg:pb-[60px]
            lg:pt-0
        `
    }
`;

export { StyleToursDetail }